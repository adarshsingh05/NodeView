{
  "build": {
    "env": {
      "NODE_ENV": "production"
    },
    "commands": {
      "install": "npm install",
      "build": "npm run build"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
